name: release

# Triggered by pushing a `v*` tag (e.g. `v0.2.11`). The tag must match the
# `<version>` in pom.xml (minus the leading `v`). Maintainers cut a release like
# this:
#
#   1. Bump `<version>` in pom.xml from `0.2.11-SNAPSHOT` to `0.2.11`, commit.
#   2. `git tag v0.2.11 && git push --tags`
#   3. (Optional) Bump pom.xml forward to `0.2.12-SNAPSHOT` and commit.
#
# The workflow then:
#   - imports a GPG private key (held in repo secret MAVEN_GPG_PRIVATE_KEY)
#   - signs all artifacts
#   - uploads to the Sonatype Central Portal using a User Token
#     (secrets CENTRAL_USERNAME / CENTRAL_PASSWORD)
#   - publishes the same version to GitHub Packages using GITHUB_TOKEN
#   - publishes a GitHub Release with the jar attached
#
# Required repo secrets (add at Settings → Secrets and variables → Actions):
#   MAVEN_GPG_PRIVATE_KEY   ASCII-armored private key, full block including
#                           BEGIN/END markers. Generate via `gpg --armor --export-secret-keys KEYID`.
#   MAVEN_GPG_PASSPHRASE    passphrase that unlocks the above key.
#   CENTRAL_USERNAME        User Token *username* from central.sonatype.com/account.
#   CENTRAL_PASSWORD        User Token *password* (token), NOT your portal password.

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write   # for creating the GitHub Release at the end
  packages: write   # for publishing to GitHub Packages

jobs:
  publish:
    name: publish to maven central
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Check required Central release secrets
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
        run: |
          set -euo pipefail
          missing=0
          for name in CENTRAL_USERNAME CENTRAL_PASSWORD MAVEN_GPG_PRIVATE_KEY MAVEN_GPG_PASSPHRASE; do
            if [ -z "${!name:-}" ]; then
              echo "::error::$name repository secret is not configured"
              missing=1
            fi
          done
          exit "$missing"

      - name: Set up JDK 17 + Maven cache + Central credentials
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
          # actions/setup-java will materialise ~/.m2/settings.xml with a <server>
          # entry whose id matches `server-id` and whose username/password are the
          # values of `server-username`/`server-password` env vars below.
          server-id: central
          server-username: CENTRAL_USERNAME
          server-password: CENTRAL_PASSWORD
          gpg-private-key: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Verify tag matches pom.xml version
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME#v}"
          pom_version=$(mvn -B -q -DforceStdout help:evaluate -Dexpression=project.version)
          echo "git tag (stripped):  $tag"
          echo "pom <version>:       $pom_version"
          if [ "$tag" != "$pom_version" ]; then
            echo "::error::tag $GITHUB_REF_NAME does not match pom version $pom_version" >&2
            exit 1
          fi

      - name: Run tests
        run: mvn -B test

      - name: Deploy to Sonatype Central Portal
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
        run: mvn -B -P publish-artifacts,release -DskipTests deploy

      - name: Verify Maven Central published artifacts
        run: |
          set -euo pipefail
          version="$(mvn -B -q -DforceStdout help:evaluate -Dexpression=project.version)"
          group_id="$(mvn -B -q -DforceStdout help:evaluate -Dexpression=project.groupId)"
          artifact_id="$(mvn -B -q -DforceStdout help:evaluate -Dexpression=project.artifactId)"
          group_path="${group_id//./\/}"
          base="https://repo.maven.apache.org/maven2/$group_path/$artifact_id/$version"
          for file in \
            "$artifact_id-$version.pom" \
            "$artifact_id-$version.jar" \
            "$artifact_id-$version-sources.jar" \
            "$artifact_id-$version-javadoc.jar"; do
            curl --fail --silent --show-error --location --retry 12 --retry-delay 10 \
              --output /dev/null "$base/$file"
          done

      - name: Set up GitHub Packages credentials
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Deploy to GitHub Packages
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mvn -B -P publish-artifacts,github-packages -DskipTests -Dgpg.skip=true deploy

      - name: Collect GitHub Release assets
        run: |
          set -euo pipefail
          version="$(mvn -B -q -DforceStdout help:evaluate -Dexpression=project.version)"
          mkdir -p dist
          cp "target/async-java-$version.jar" dist/
          cp "target/async-java-$version-sources.jar" dist/
          cp "target/async-java-$version-javadoc.jar" dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: dist/*
